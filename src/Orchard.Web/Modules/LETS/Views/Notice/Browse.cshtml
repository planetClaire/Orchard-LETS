@model BrowseNoticeCategoriesViewModel
@using LETS.ViewModels

<header class="page-header">
    <h1>@T("Browse Categories")</h1>
</header>
<div class="row">
    @foreach (var browseNoticeCategoryViewModel in Model.Categories)
    {
        <div class="col-md-3">
            <h3>@browseNoticeCategoryViewModel.Term.Name</h3>
            <ul class="subCategories browseCategories">
                @foreach(var subCategory in browseNoticeCategoryViewModel.SubCategories)
                {
                    var className = "empty";
                    var noticeCountLabel = "";
                    var noticeCount = subCategory.NoticeCount;
                    if (noticeCount > 0) {
                        className = "";
                        noticeCountLabel = string.Format(" ({0})", noticeCount);
                    }
                    <li class="@className">
                        @Html.ItemDisplayLink(string.Format("{0}{1}", subCategory.Term.Name, noticeCountLabel), subCategory.Term.ContentItem)
                    </li>
                }
            </ul>
        </div>
    }
</div>